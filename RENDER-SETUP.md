# 🚀 Деплой бота на Render

## Быстрый старт

### 1. Создайте аккаунт на Render

Перейдите на **[render.com](https://render.com)** и войдите через GitHub

### 2. Создайте новый Web Service

1. На главной странице нажмите **"New +"**
2. Выберите **"Web Service"**
3. Подключите ваш GitHub репозиторий **Ansagan94/bot**
4. Нажмите **"Connect"**

### 3. Настройте параметры деплоя

Заполните форму:

**Basic Settings:**
- **Name**: `telegram-bot-kvartir` (или любое другое имя)
- **Region**: `Frankfurt (EU Central)` (ближайший к вам)
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: `Node`

**Build & Deploy:**
- **Build Command**: `npm install`
- **Start Command**: `node index.js`

**Instance Type:**
- Выберите **"Free"** ($0/месяц)

### 4. Добавьте переменные окружения

В разделе **"Environment Variables"** нажмите **"Add Environment Variable"**:

- **Key**: `BOT_TOKEN`
- **Value**: `8574059700:AAGp7zDVEWgQ82WksSWlNEv6a1yLdh9cd_o`

Нажмите **"Add"**

### 5. Создайте сервис

Нажмите **"Create Web Service"**

Render автоматически:
- Установит зависимости
- Запустит бота
- Будет держать его работающим 24/7

## ✅ Готово!

Через 2-3 минуты ваш бот будет работать на Render!

Проверьте: https://t.me/Kvart01_bot

---

## Управление ботом на Render

### Просмотр логов
1. Откройте ваш сервис на Render
2. Перейдите во вкладку **"Logs"**
3. Увидите сообщение: `🤖 Бот запущен и готов к работе!`

### Перезапуск бота
1. Перейдите во вкладку **"Manual Deploy"**
2. Нажмите **"Deploy latest commit"**

### Автоматический деплой
При каждом `git push` в GitHub, Render автоматически обновит бота!

### Изменить переменные окружения
1. Перейдите во вкладку **"Environment"**
2. Измените `BOT_TOKEN` при необходимости
3. Сохраните — бот перезапустится автоматически

---

## Ограничения Free плана

- ✅ Бесплатно навсегда
- ✅ 750 часов в месяц (достаточно для одного бота 24/7)
- ⚠️ Бот засыпает после 15 минут неактивности
- ⚠️ При получении сообщения бот "просыпается" за ~30 секунд

### Решение проблемы засыпания

Если нужно, чтобы бот отвечал мгновенно, используйте внешний сервис пингов (например, [cron-job.org](https://cron-job.org)):

1. Зарегистрируйтесь на cron-job.org
2. Создайте задачу, которая пингует ваш URL каждые 10 минут
3. URL вашего сервиса: `https://ВАШЕ_ИМЯ.onrender.com`

---

## Альтернатива: Background Worker

Если не нужен веб-сервер, можно использовать Background Worker (он не засыпает):

1. При создании сервиса выберите **"Background Worker"** вместо **"Web Service"**
2. Start Command: `node index.js`
3. Всё остальное — так же

**Преимущества Background Worker:**
- ✅ Не засыпает
- ✅ Отвечает мгновенно
- ⚠️ Требует платный план ($7/месяц)

---

## Проблемы и решения

### Бот не отвечает
1. Проверьте логи на Render
2. Убедитесь, что `BOT_TOKEN` правильный
3. Проверьте, что сервис запущен (статус: **"Live"**)

### Ошибка "409 Conflict"
Это значит, что бот запущен в нескольких местах. Остановите локальную версию или другие инстансы.

### Обновить бота
```bash
git add .
git commit -m "Update bot"
git push origin main
```
Render автоматически задеплоит изменения.

---

## Преимущества Render

✅ **Простой деплой** — подключите GitHub и всё  
✅ **Бесплатный план** — навсегда  
✅ **Авто-деплой** — при каждом push  
✅ **Логи в реальном времени**  
✅ **SSL сертификат** (если нужен)  

---

## Полезные ссылки

- **Dashboard**: https://dashboard.render.com
- **Документация**: https://render.com/docs
- **Ваш репозиторий**: https://github.com/Ansagan94/bot
- **Ваш бот**: https://t.me/Kvart01_bot
